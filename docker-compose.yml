version: "3.8"
services:
  workerbee:
    image: node:18
    # If you want to run a placeholder, just keep this:
    command: tail -f /dev/null
    volumes:
      - ./:/app
    working_dir: /app
    restart: unless-stopped
