services:
  workerbee:
    image: node:18
    command: sh -c "npm install && node scripts/workerbee-quote-server.mjs"
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "18810:18910"
    environment:
      - WORKERBEE_PORT=18910
    restart: unless-stopped
